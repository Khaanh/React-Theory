import React from "react";
import Mouse from "./Mouse";

const cardStyle = {
  height: "100px",
  border: "1px solid black",
  margin: "5px",
};

const Card = (props) => {
  return;
};

export default Card;
